# Contributing

贡献内容应保留 Work Wiki 默认的隐私姿态。

## Rules

- 公开的来源注册表示例使用 `.example.md`。
- 使用别名，不要使用真实本地路径、账号名、tenant 名、ticket key 或私有 provider 标识符。
- 不要向示例中加入原始邮件、聊天、文档、监控、事故或代码证据。
- 保持协议 Markdown-first 且 agent-portable。
- 新增来源类型或模板时，同步更新验证指南。
